build/
build-*/
out/
cmake-build-*/
.cache/
compile_commands.json
*.o
*.a
*.so
*.exe
.vscode/
.idea/

# FetchContent, shared between CI jobs so it can be cached
.deps/

# Coverage and static analysis output.  None of it is committed: the coverage
# badge and its history live at codecov.io, and the analyser reports are build
# artefacts that CI uploads.
*.gcov
*.gcda
*.gcno
coverage.json
cobertura.xml
clang-tidy.log
cppcheck.xml

# Scratch: things moved aside rather than deleted outright
_to_delete/

# Python bytecode from tools/, and ruff's cache
__pycache__/
*.pyc
.ruff_cache/
