// SPDX-License-Identifier: MIT
//
// Generated by CMake from include/fms/version.hpp.in.  Do not edit, and do not
// commit the result: the version is declared once, by project() in
// CMakeLists.txt, so a header and a release tag cannot say different things.
#ifndef FMS_VERSION_HPP
#define FMS_VERSION_HPP

#define FMS_VERSION_MAJOR  @PROJECT_VERSION_MAJOR@
#define FMS_VERSION_MINOR  @PROJECT_VERSION_MINOR@
#define FMS_VERSION_PATCH  @PROJECT_VERSION_PATCH@
#define FMS_VERSION_STRING "@PROJECT_VERSION@"

/// Comparable form, for `#if FMS_VERSION >= FMS_VERSION_AT(1, 0, 0)`.
#define FMS_VERSION_AT(major, minor, patch) ((major) * 10000 + (minor) * 100 + (patch))
#define FMS_VERSION FMS_VERSION_AT(FMS_VERSION_MAJOR, FMS_VERSION_MINOR, FMS_VERSION_PATCH)

namespace fms {

/// The version of the headers this translation unit compiled against.  For a
/// static library that is also the version of the code it links: fms/abi.hpp
/// makes a build from two different trees fail to link.
constexpr const char* version() noexcept { return FMS_VERSION_STRING; }

}  // namespace fms

#endif  // FMS_VERSION_HPP
